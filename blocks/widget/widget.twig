<section class="{{ classes|join(' ') }} col-12 col-sm-6 col-md-4 col-lg">
    {% if fields.title %}
        <h2 class="widget__title">{{ fields.title }}</h2>
    {% endif %}

    {% if fields.type == 'menu' and widget_navigation %}
        {{ widget_navigation }}
    {% endif %}
    {{ InnerBlocks }}
</section>
